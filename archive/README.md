# Historical Prototypes

Files in this directory preserve early morsePi experiments for project history.
They are not imported, deployed, or used by the current application.

These prototypes may display canonical ASCII Morse with ordinary periods and
hyphens. The maintained application uses the shared centered-symbol renderer
for student-facing Morse while continuing to store and compare canonical `.`
and `-` values internally.
